# 中关村学院建大楼 GDD

## 1. 项目定位

### 1.1 游戏名称

暂定名：**中关村学院：Build The Tower**

### 1.2 改造目标

在现有 `zgca-2048` 项目内直接改造，将“合成楼栋”的 2048 玩法替换为“吊机落块建塔”的叠叠乐玩法，同时最大化保留参考项目中的视觉设计、学院包装、移动端布局、进度解锁、楼栋介绍、本地存档和通关登记能力。

### 1.3 核心体验

玩家在中关村学院的施工现场中，通过点击屏幕让吊机释放摇晃中的楼栋模块。模块落到下方塔楼后，根据与上一层的水平重合程度判定为 `Perfect`、`Good` 或 `Miss`。重合越好，塔楼越稳、容纳人数越多、楼栋介绍解锁越快；累计 3 次 `Miss` 后建塔失败。

### 1.4 目标平台

- 主要平台：移动端浏览器。
- 次要平台：桌面浏览器。
- 技术延续：Vite + TypeScript + 原生 DOM/CSS。
- 画布延续：保留现有 440px 宽移动端适配方案。

## 2. 参考项目保留策略

### 2.1 保留内容

- 保留学院主题：`ZGC Academy`、中关村学院、C1/C2/C3/C5/C7/C8/C9。
- 保留楼栋介绍配置：继续使用 `buildingConfig.ts` 和 `buildingIntroConfig.ts` 的思路。
- 保留进度条体验：将“合成解锁进度”改为“建塔高度解锁进度”。
- 保留视觉基调：圆角卡片、明亮渐变、厚边框、软阴影、轻量弹性动画。
- 保留移动端缩放：继续使用 `designWidth = 440` 和 `--mobile-scale` 方案。
- 保留教程弹窗、结果弹窗、重新开始按钮、最高纪录本地存储。
- 保留远端通关登记：达成最终楼栋 C9 或指定高度时登记通关。

### 2.2 替换内容

- 将 4x4 棋盘替换为竖向施工场景。
- 将方向键/滑动输入替换为点击/空格释放方块。
- 将 2048 合并分数替换为“容纳人数”。
- 将“没有可移动方块”失败条件替换为“累计 3 次 Miss”。
- 将方块数值等级替换为楼层、稳定度和楼栋解锁节点。

## 3. 世界观与主题包装

### 3.1 背景设定

中关村学院正在进行一场象征性的校园建设挑战。玩家扮演“新生建设官”，用吊机把一层层学院模块精准叠起，逐步完成学院空间的修建。每一段高度代表学院的一处关键楼栋或功能区域被点亮。

### 3.2 包装关键词

- 建塔：模拟学院成长。
- 容纳人数：替代传统分数，表现学院承载能力。
- 稳定度：表现塔楼晃动风险。
- 楼栋解锁：沿用 C1 至 C9 的收集/介绍动机。
- 施工现场：吊机、缆绳、模块、整片地面、云朵、校园远景。

### 3.3 楼栋节点

继续保留 C1 到 C9 中间有空的节点序列：

- C1：新手起点，第一段楼层与宿舍印象。
- C2：初步建成，第一届学生入住。
- C3：校园生活开始展开。
- C5：学院行政与公共活动核心。
- C7：办公空间逐渐启用。
- C8：学服、图书馆、健身、舞蹈等生活功能完善。
- C9：学院拼图完成，视为通关目标。

### 3.4 阶段颜色主题

每一个建设阶段都需要有独立的颜色主题，用来区分当前正在建设的学院区域。主题色同时影响楼栋 block、当前方块、进度节点、介绍卡点缀色和施工背景氛围，让玩家不只通过文字知道阶段变化，也能通过画面颜色感受到“进入了新的楼栋阶段”。

建议主题：

- C1：晨光蓝。表现新手起点、首层与宿舍初建，block 使用蓝白渐变，背景偏清晨天空。
- C2：新芽绿。表现第一批学生入住和校园开始生长，block 使用绿色到青色渐变，背景加入草地和树影感。
- C3：活力橙。表现校园生活与下沉商业区，block 使用橙黄渐变，背景偏暖色街区灯光。
- C5：核心粉。表现行政、会议和学院心脏，block 使用粉红到玫红渐变，背景更明亮热闹。
- C7：办公紫。表现办公楼与新空间启用，block 使用紫色到靛蓝渐变，背景加入现代办公光带。
- C8：服务青。表现学服、图书馆、健身房和舞蹈室，block 使用青色到薄荷绿渐变，背景更清爽开放。
- C9：完成金。表现最终拼图和学院建成，block 使用金黄到橙红渐变，背景加入庆祝光晕。

主题切换规则：

- 当前高度达到某个楼栋节点后，后续新生成 block 使用该节点主题。
- 已经叠上的旧 block 保留落下时所属阶段颜色，形成从下到上的学院成长色带。
- 背景使用“当前最高已解锁阶段”的主题渐变，切换时做 300ms 到 500ms 的柔和过渡。
- C9 解锁后背景进入完成态，可增强光晕、彩带或远景建筑亮灯效果。

## 4. 核心玩法

### 4.1 单局循环

1. 屏幕上方出现吊机和悬挂方块。
2. 方块随吊机或缆绳进行横向周期摇晃。
3. 玩家点击屏幕或按空格释放方块。
4. 方块竖直下落，与当前塔顶进行碰撞/重合判定。
5. 系统根据重合程度给出结果反馈。
6. 成功或部分成功时，方块成为新的塔顶，塔楼高度增加。
7. 生成下一块，吊机继续摇晃。
8. 到达高度节点时解锁楼栋介绍。
9. 累计 3 次 Miss 或完成 C9 后进入结算。

### 4.2 输入方式

- 移动端：点击主施工区域释放方块。
- 桌面端：鼠标点击或空格键释放方块。
- 教程按钮：复用现有 `?` 帮助按钮。
- 重新开始：复用现有底部按钮与结果弹窗按钮。

### 4.3 方块生成

每个方块代表一层学院模块。

- 初始宽度：建议为场景宽度的 46% 到 54%。
- 高度：固定，便于形成清晰楼层堆叠。
- 外观：沿用现有 tile 的厚边框、渐变色和高光，可加入窗户/楼栋标签。
- 标签：普通楼层显示“新楼层”“教室模块”等；解锁节点层可显示 C1/C2 等。
- 颜色：根据当前最高已解锁建设阶段取主题色，保证 C1、C7 等不同阶段的 block 有明显视觉差异。

### 4.4 吊机运动

基础版本采用横向正弦或往返运动：

- `craneX = center + sin(time * speed) * amplitude`
- 当前方块跟随 `craneX` 移动。
- 随高度提升，速度和幅度逐步增加。
- 塔楼稳定度低时，可叠加轻微相位扰动，让瞄准更紧张。
- 施工镜头不固定在初始地面位置，而是随塔楼高度平滑上移。
- 镜头跟随对象需要覆盖“当前塔顶 + 整台吊机”，保证吊机主体、缆绳和落点区域始终在可视范围内。
- 每次成功叠高后，镜头以插值方式继续上推，避免玩家感觉画面突跳或吊机被截断。

### 4.5 落块判定

方块落下后，计算当前方块与承托对象在 X 轴上的重合宽度。第一块的承托对象是整片地面，后续方块的承托对象是塔顶方块；场景中不再额外生成一个“地基 block”。

- `overlapWidth = min(blockRight, topRight) - max(blockLeft, topLeft)`
- `overlapRatio = overlapWidth / blockWidth`
- `offsetRatio = abs(blockCenterX - topCenterX) / blockWidth`

判定建议：

- `Perfect`：`overlapRatio >= 0.9`
- `Good`：`0.45 <= overlapRatio < 0.9`
- `Miss`：`overlapRatio < 0.45` 或完全没有重合

### 4.6 方块裁切方案

为降低实现风险，建议第一版不做真实裁切，采用“逻辑裁切 + 视觉反馈”：

- `Perfect`：新方块完整保留，塔楼稳定度提升。
- `Good`：新方块完整保留，但记录偏移，塔楼摇晃增加。
- `Miss`：方块掉落到屏幕外，不加入塔楼，miss 次数 +1。

增强版可以加入真实裁切：

- 成功落下后，只保留与塔顶重合的部分作为新塔顶。
- 未重合的悬空部分播放掉落动画。
- 随层数增加，方块可能越来越窄，形成传统叠叠乐压力。

### 4.7 塔楼摇晃

塔楼摇晃既是视觉反馈，也是难度提示。

- `Perfect`：减少摇晃值。
- `Good`：根据偏移量增加摇晃值。
- `Miss`：明显增加背景震动或吊机晃动，但塔楼不新增层。
- 高层：基础摇晃逐渐增加。
- 摇晃只影响表现和瞄准压力，第一版不建议因为摇晃直接导致倒塌，避免失败过于随机。

## 5. 数值设计

### 5.1 核心状态

- `towerBlocks`：已成功叠上的楼层列表。
- `currentBlock`：吊机当前悬挂的方块。
- `height`：当前成功楼层数。
- `capacity`：容纳人数，替代原 `score`。
- `bestCapacity`：最高容纳人数，替代原 `bestScore`。
- `missCount`：当前 Miss 次数。
- `maxMissCount`：固定为 3。
- `stability`：稳定度，0 到 100。
- `swayIntensity`：视觉摇晃强度。
- `unlockedLevels`：已解锁楼栋节点。
- `selectedLevel`：当前查看的楼栋介绍节点。

### 5.2 容纳人数计算

每成功叠上一层，按照重合度发放容纳人数。

建议公式：

- 基础人数：`baseCapacity = 20 + height * 2`
- 重合倍率：`overlapMultiplier = 0.6 + overlapRatio * 0.8`
- 连续 Perfect 加成：`perfectBonus = perfectCombo * 5`
- 单层得分：`floorCapacity = floor((baseCapacity * overlapMultiplier + perfectBonus) / 5)`

结果包装：

- UI 不显示“分数”，显示“容纳人数”。
- 最高分显示为“最高容纳”。
- 结算文案显示“本次学院可容纳 X 人”。

### 5.3 判定奖励

- `Perfect`
  - 容纳人数倍率高。
  - `perfectCombo +1`。
  - 稳定度 +4，最高不超过 100。
  - 播放金色闪光与“精准就位”文案。
- `Good`
  - 容纳人数按重合度计算。
  - `perfectCombo` 清零。
  - 稳定度按偏移减少。
  - 播放普通落位反馈。
- `Miss`
  - 不增加容纳人数。
  - `missCount +1`。
  - `perfectCombo` 清零。
  - 稳定度 -12。
  - 播放掉落、震屏和“施工偏差”文案。

### 5.4 难度曲线

建议按高度增加难度：

- 0 到 5 层：吊机速度低，幅度小，教学阶段。
- 6 到 12 层：速度略升，开始要求节奏判断。
- 13 到 22 层：幅度增加，Good 更容易造成摇晃。
- 23 层以后：速度和摇晃都明显提升，Perfect 连击成为高分关键。

可用参数：

- `swaySpeed = baseSpeed + height * 0.015`
- `swayAmplitude = min(maxAmplitude, baseAmplitude + height * 1.2)`
- `fallSpeed = baseFallSpeed + height * 0.25`

## 6. 解锁与进度

### 6.1 解锁方式

由原来的“合成到指定数值”改为“成功建到指定高度”。

建议节点：

- C1：1 层，进入学院建设。
- C2：4 层，完成第一段宿舍。
- C3：8 层，校园生活区域出现。
- C5：12 层，行政与公共活动空间启用。
- C7：17 层，办公楼启用。
- C8：23 层，服务与生活功能完善。
- C9：30 层，学院建设完成，触发通关。

### 6.2 进度条

保留现有横向 7 节点进度条。

- 标题从“楼栋解锁进度”调整为“学院建设进度”。
- 节点仍显示 C1/C2/C3/C5/C7/C8/C9。
- 未解锁节点显示 `?`。
- 新解锁时自动选中该节点并更新介绍卡。

### 6.3 楼栋介绍

继续沿用现有介绍内容，但文案可以从“收集楼栋”调整为“建设阶段”：

- C1：首层落成，宿舍楼点亮。
- C2：第一届学生入住。
- C3：生活与商业区出现。
- C5：学院行政与公共活动心脏。
- C7：办公空间开始运转。
- C8：学服、图书馆、健身房等功能完善。
- C9：最后一块拼图完成。

## 7. UI/UX 设计

### 7.1 页面结构

建议沿用现有布局顺序，并替换中部核心区域：

1. 顶部栏：标题、容纳人数、最高容纳、帮助按钮。
2. 远端通关状态：如 URL 中有用户 ID，则保留显示。
3. 学院建设进度：7 节点进度条。
4. 楼栋介绍卡：当前解锁楼栋说明。
5. 施工场景：吊机、悬挂方块、塔楼、整片地面、判定反馈。
6. 底部操作栏：重新开始、提示文字。
7. 教程弹窗与结果弹窗。

### 7.2 施工场景元素

- 背景：延续当前糖果色天空，可加入校园远景剪影，并根据当前建设阶段切换主题渐变；背景视窗随建塔进度整体上移，而不是固定在初始高度。
- 吊机：位于施工视窗顶部附近，随镜头和修建高度整体上移，同时继续做横向移动或吊臂摆动。
- 缆绳：连接吊机和当前方块，可随吊机轻微倾斜。
- 当前方块：有高光、边框、楼层纹理，并使用当前阶段主题色。
- 塔楼：由成功方块堆叠而成，整体随稳定度轻微摆动，并作为镜头持续上移的主要参考对象。
- 地面：底部整片施工地面，作为第一块的承托范围，可显示学院施工标识。
- 判定文字：落点附近弹出 `Perfect`、`Good`、`Miss`。

### 7.3 阶段视觉主题

阶段主题需要让玩家在不看进度文字时，也能辨认当前建到了哪一类学院空间。每个阶段至少定义 4 类颜色：`blockGradient`、`backgroundGradient`、`accentColor`、`glowColor`。

- C1：block 为蓝白，背景为清晨蓝天，点缀色为天蓝。
- C2：block 为新芽绿，背景为浅绿校园，点缀色为草绿色。
- C3：block 为橙黄，背景为暖橙街区，点缀色为琥珀色。
- C5：block 为粉红玫红，背景为粉金学院中庭，点缀色为玫红。
- C7：block 为紫色靛蓝，背景为蓝紫办公夜光，点缀色为紫色。
- C8：block 为青色薄荷绿，背景为清爽图书馆/活动空间，点缀色为青绿色。
- C9：block 为金黄橙红，背景为庆祝金光，点缀色为金色。

背景不需要彻底换场景，第一版可以通过 CSS 变量切换渐变和装饰光斑颜色完成。这样能保留参考项目的统一 UI 风格，同时强化每个建设阶段的辨识度。

### 7.4 HUD 文案

- 游戏标题：`中关村学院建大楼`
- 英文眉标：`ZGC Academy`
- 当前分数：`容纳人数`
- 最高分：`最高容纳`
- Miss：`施工偏差 0/3`
- 稳定度：`塔楼稳定度`
- 底部提示：`点击屏幕，让学院模块落位`

### 7.5 教程文案

标题：欢迎来到中关村学院建大楼

正文建议：

- 点击屏幕，释放吊机上的学院模块。
- 模块越对齐，容纳人数越高，塔楼越稳定。
- 累计 3 次 miss，挑战失败。
- 建到 C9，完成中关村学院建设。

### 7.6 结算文案

胜利：

- 眉标：`建设完成`
- 标题：`解锁 C9，学院建成！`
- 正文：`你已经完成中关村学院的建设挑战，本次学院可容纳 X 人。`

失败：

- 眉标：`建设中止`
- 标题：`累计 3 次 miss，挑战失败`
- 正文：`本次学院可容纳 X 人，再来一次，把塔楼叠得更稳。`

## 8. 反馈与表现

### 8.1 判定反馈

- `Perfect`：金黄色爆闪、粒子、音效高亮、短暂慢动作。
- `Good`：蓝绿色落位光效、轻微弹跳。
- `Miss`：红色提示、方块坠落、屏幕轻震。

### 8.2 动画节奏

- 方块悬挂：持续轻微上下浮动。
- 方块下落：快速但可读，落地前有重影。
- 成功落位：塔楼弹一下。
- 解锁节点：进度节点发光，介绍卡刷新。
- 游戏结束：背景压暗，结果卡弹出。

### 8.3 音效建议

第一版可以不接入音频；若添加，建议使用短音效：

- 吊机移动环境声：低音量循环。
- 释放：轻快机械声。
- Perfect：明亮提示音。
- Good：落块声。
- Miss：坠落和警示声。
- 解锁：校园铃声或上升音阶。

## 9. 技术设计建议

### 9.1 文件沿用

- `src/main.ts`：重写核心状态、输入、渲染与游戏循环。
- `src/styles.css`：保留视觉基调，替换棋盘样式为施工场景样式。
- `src/buildingConfig.ts`：将 `level` 从 2048 数值含义改为高度节点，或新增 `heightRequired`，并为每个建设阶段配置颜色主题。
- `src/buildingIntroConfig.ts`：保留并调整文案。
- `index.html`：标题改为 `中关村学院建大楼`。

### 9.2 状态类型建议

```ts
type JudgeResult = "perfect" | "good" | "miss";

type TowerBlock = {
  id: number;
  x: number;
  y: number;
  width: number;
  height: number;
  label?: string;
  themeId: string;
  judge: JudgeResult | "base";
};

type GameState = {
  towerBlocks: TowerBlock[];
  currentBlock: TowerBlock;
  capacity: number;
  bestCapacity: number;
  missCount: number;
  stability: number;
  perfectCombo: number;
  unlockedLevels: number[];
  selectedLevel: number;
  currentThemeId: string;
  isWon: boolean;
  isGameOver: boolean;
  lastJudge: JudgeResult | null;
};
```

建设阶段配置建议：

```ts
type StageTheme = {
  id: string;
  blockGradient: string;
  backgroundGradient: string;
  accentColor: string;
  glowColor: string;
};

type BuildingConfigItem = {
  id: string;
  level: number;
  heightRequired: number;
  name: string;
  title: string;
  icon: string;
  lockedIcon: string;
  isProgressNode: boolean;
  theme: StageTheme;
};
```

### 9.3 游戏循环

需要从“用户输入驱动移动”改为“动画帧驱动场景”。

- 使用 `requestAnimationFrame(tick)`。
- tick 中更新吊机位置、当前方块位置、落块动画、塔楼摇晃和镜头偏移。
- 镜头偏移量根据当前最高有效楼层与吊机顶部位置计算，并通过平滑插值跟随，确保整台吊机不会脱离画面。
- 点击时只改变当前方块状态：`hanging -> falling`。
- 下落完成后进行判定并生成下一块。

### 9.4 渲染方式

为了尽量沿用现有项目，可以继续使用字符串模板渲染 DOM。

注意事项：

- 高频动画不建议每帧重建整个 `app.innerHTML`。
- 建议将静态 UI 和施工场景拆分，动画元素通过 CSS transform 更新。
- 第一版如实现简单，也可以每次状态变化后重新 render，吊机摇晃使用 CSS animation 承担。

### 9.5 存储键建议

旧版键名为 `zgca-2048-*`，新版本建议换为：

- `zgca-tower-best-capacity`
- `zgca-tower-unlocked-levels`
- `zgca-tower-tutorial-seen`

这样可避免旧 2048 存档污染新玩法。

### 9.6 远端通关

保留 API 结构，建议将 `gameId` 从 `zgca-2048` 调整为：

- `zgca-build-the-tower`

通关触发条件：

- 达到 C9 高度节点。
- 或后续运营要求的指定高度。

## 10. MVP 范围

### 10.1 必做

- 点击释放方块。
- 吊机横向摇晃。
- 镜头随塔高和吊机整体平滑上移。
- 方块下落与重合判定。
- `Perfect`、`Good`、`Miss` 三种反馈。
- 3 次 Miss 失败。
- 容纳人数与最高容纳。
- C1/C2/C3/C5/C7/C8/C9 高度解锁。
- 教程、重新开始、胜利/失败结算。
- 移动端适配。

### 10.2 可延后

- 真实方块裁切。
- 音效。
- 粒子系统。
- 更复杂的物理倒塌。
- 排行榜展示。
- 多种方块皮肤。

### 10.3 不建议第一版加入

- 完整刚体物理模拟。
- 随机风力导致的强制偏移。
- 因稳定度低而自动失败。
- 多吊机、多塔楼等复杂机制。

## 11. 验收标准

### 11.1 玩法验收

- 玩家无需阅读复杂说明即可理解“等对齐后点击”。
- 每次落块结果清晰可见。
- Perfect 明显比 Good 更爽。
- Miss 有压力，但 3 次容错能鼓励继续挑战。
- 到达新高度节点时，楼栋介绍能自然成为奖励。

### 11.2 包装验收

- UI 一眼能看出延续自参考项目。
- 学院楼栋 C1/C2/C3/C5/C7/C8/C9 仍是主要收集目标。
- 分数包装为“容纳人数”后语义自然。
- 胜利目标从“合成 C9”自然迁移为“建到 C9”。

### 11.3 技术验收

- `npm run build` 通过。
- 移动端宽度 320px 到 440px 可正常游玩。
- 桌面端鼠标和空格可正常操作。
- 本地最高容纳与解锁进度可持久化。
- 达成 C9 后可触发通关登记逻辑。

## 12. 后续迭代方向

- 加入真实裁切，使塔楼宽度随失误缩窄。
- 加入学院功能模块皮肤，例如宿舍、教室、图书馆、办公区。
- 加入每日挑战：固定吊机速度和方块序列，比拼容纳人数。
- 加入“建设报告”结算页，展示 Perfect 次数、最高连击、最大高度和稳定度。
- 加入隐藏彩蛋：连续 Perfect 解锁特殊学院标识或庆祝动画。
